var num = Number(prompt("Enter the Number: "))
var n = parseInt(Math.sqrt(num))
// var n = parseInt(num ** 0.5)
if (n * n == num){

    // if (Number.isInteger(num ** 0.5))
        document.writeln(`${num} is Perfect Square`)
}
    else{
        
    }

        document.writeln(`${num} is not a Perfect Square`)


